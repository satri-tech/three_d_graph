## 0.1.0

* Fix: `drawPolygon3D` now normalizes vertex coordinates to match `drawPoint3D` scaling
* Feat: Added `example/` directory with interactive shape explorer app
* Feat: Added color picker with presets and HSV customization
* Feat: Added render mode toggle (points / shape / both)
* Chore: Cleaned up code, removed deprecated `withOpacity` usage

## 0.0.1

* Initial release with `ThreeDGraph` widget and `Shapes` class
